import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  text: {
    fontSize: 30,
    color: 'white',
    backgroundColor: 'pink',
    padding: 16,
    margin: 16,
    textAlign: 'center',
    borderColor: 'blue',
    borderWidth: 2,
  },
});

// Ab isko use karne k liye export karana zaruri hai
export default style;
